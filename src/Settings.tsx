import { Text } from "@fluentui/react-components";

function Settings()
{
    return (
        <Text>Settings Menu (this needs to be seperate from the rest of the menu)</Text>
    )
}

export default Settings;